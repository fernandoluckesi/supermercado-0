version: '3'

services:
  postgres:
    container_name: meli_supermercado_postgres
    image: postgres:13
    ports:
      - 5432:5432
    environment: 
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=meli_supermercado